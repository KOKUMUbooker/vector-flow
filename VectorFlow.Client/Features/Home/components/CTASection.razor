<div class="cta-section">
    <MudContainer MaxWidth="MaxWidth.Large" Class="text-center">
        <MudText Typo="Typo.h2" Style="font-family: var(--vf-serif); margin-bottom: 0.75rem;">Ready to track what matters?</MudText>
        <MudText Typo="Typo.body1" Style="margin-bottom: 2rem;">
            VectorFlow is for small dev teams who want to stay focused, not buried in tooling.
        </MudText>
        <div class="cta-actions">
            <MudButton Variant="Variant.Filled" Color="Color.Primary" Size="Size.Large" Href="/register">
                Create your workspace <MudIcon Icon="@Icons.Material.Filled.ArrowForward" Size="Size.Small" />
            </MudButton>
            <MudButton Variant="Variant.Outlined" Color="Color.Default" Size="Size.Large" Href="/login">
                Sign in
            </MudButton>
        </div>
    </MudContainer>
</div>

<style>
     /* CTA Section */
    .cta-section {
        padding: 80px 0;
        border-top: 1px solid rgba(67,57,202,0.12);
        border-bottom: 1px solid rgba(67,57,202,0.12);
    }
    
    .cta-actions {
        display: flex;
        gap: 12px;
        justify-content: center;
        flex-wrap: wrap;
    }
</style>